components:
  schemas:
    CreateTaskInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CreateTaskInputBody.json
          format: uri
          readOnly: true
          type: string
        notes:
          description: Optional longer detail
          maxLength: 2000
          type: string
        title:
          description: Short task title
          maxLength: 200
          minLength: 1
          type: string
      required:
        - title
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          type: string
        message:
          description: Error message text
          type: string
        value:
          description: The value at the given location
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ErrorModel.json
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    ListTasksOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListTasksOutputBody.json
          format: uri
          readOnly: true
          type: string
        active:
          description: "Active pool: Pending and In Progress tasks, manually ordered"
          items:
            $ref: "#/components/schemas/TaskDTO"
          type:
            - array
            - "null"
      required:
        - active
      type: object
    TaskDTO:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TaskDTO.json
          format: uri
          readOnly: true
          type: string
        completed_at:
          description: When the task was completed, if it has been
          format: date-time
          type: string
        created_at:
          description: When the task was created
          format: date-time
          type: string
        deleted_at:
          description: When the task was cancelled, if it has been
          format: date-time
          type: string
        id:
          description: Unique task identifier
          format: int64
          type: integer
        notes:
          description: Optional longer detail
          type: string
        position:
          description: Manual ordering position within the active pool
          format: int64
          type: integer
        status:
          description: Lifecycle status
          enum:
            - pending
            - in_progress
            - completed
          type: string
        title:
          description: Short task title
          type: string
      required:
        - id
        - title
        - notes
        - status
        - position
        - created_at
      type: object
info:
  title: Tasky API
  version: 1.0.0
openapi: 3.1.0
paths:
  /tasks:
    get:
      description: "Return the active pool: Pending and In Progress tasks, manually ordered."
      operationId: listTasks
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListTasksOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List active tasks
      tags:
        - tasks
    post:
      description: Create a task from a title and optional notes. A created task starts as Pending.
      operationId: createTask
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTaskInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskDTO"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a task
      tags:
        - tasks
